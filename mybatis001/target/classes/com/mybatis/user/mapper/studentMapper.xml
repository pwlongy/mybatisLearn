<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.user.mapper.studentMapper">
    <resultMap id="studentMap" type="student">

    </resultMap>
    
    <insert id="insertStudent" parameterType="student" useGeneratedKeys="true" keyProperty="id">
        insert into student
            (name, age, gender, class_name, score, phone, create_time)
        values
            (#{name}, #{age}, #{gender}, #{className}, #{score}, #{phone}, #{createTime} )
    </insert>

    <!--
        当数据库字段和在java中的命名不一致的时候，名字对应不上的将无法返回数据，一共有三种方式解决这个问题
        1. as 给列起别名
        2. resultMap 映射
        3. 开启驼峰命名自定映射（配置mybatis-config.xml 中的 setting）
    -->
    <select id="selectStudentName" parameterType="String" resultType="student">
        select name, age, gender, class_name as className, score, phone, create_time as createTime
        from student
        <if test="name != null">
            where name like concat('%', #{name}, '%')
        </if>
    </select>
</mapper>